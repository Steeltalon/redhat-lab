---
# tasks file for apache
- name: Deploy Apache
  hosts: apps
  gather_facts: false
  become: true

  tasks:
  - name: Install apache
    yum:
      name: httpd
      state: latest

  - name: Enable apache at boot
    service:
      name: httpd
      enabled: yes
      notify: Start Apache
